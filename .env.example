# Sanity CMS Configuration
# Note: Sanity configuration (projectId, dataset, apiVersion) is hardcoded in src/sanity/client.ts
# These values are public by design in Sanity CMS

# Only needed if your Sanity dataset is private (requires authentication)
# SANITY_API_READ_TOKEN=your_read_token_here
